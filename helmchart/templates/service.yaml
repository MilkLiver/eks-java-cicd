apiVersion: v1
kind: Service
metadata:
  name: {{ include "eks-java-cicd.fullname" . }}
  labels:
    {{- include "eks-java-cicd.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "eks-java-cicd.selectorLabels" . | nindent 4 }}
